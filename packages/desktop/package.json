{
  "name": "@agenfk/desktop",
  "version": "1.1.18",
  "private": true,
  "description": "AgEnFK Desktop \u2014 run agent orchestration as a native app",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && electron dist/main/index.js",
    "start": "electron dist/main/index.js",
    "pack": "npm run build && npm run bundle:server && electron-builder --dir --config electron-builder.config.cjs",
    "dist:mac": "npm run build && npm run bundle:server && electron-builder --mac --config electron-builder.config.cjs",
    "dist:win": "npm run build && npm run bundle:server && electron-builder --win --config electron-builder.config.cjs",
    "dist:linux": "npm run build && npm run bundle:server && electron-builder --linux --config electron-builder.config.cjs",
    "bundle:server": "node scripts/bundle-server.mjs"
  },
  "dependencies": {
    "@agenfk/telemetry": "1.1.18"
  },
  "devDependencies": {
    "electron": "40.10.6",
    "typescript": "~5.9.3",
    "@types/node": "^22.10.2",
    "electron-builder": "^26.0.12"
  },
  "author": "CG/lab"
}
