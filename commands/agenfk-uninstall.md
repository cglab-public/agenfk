---
description: Uninstall AgenFK — removes MCP config, slash commands, skills, and framework files
---

You are executing the `/agenfk-uninstall` command.

Run the uninstall script with `-y` to skip the interactive prompt:

```bash
node ~/.agenfk-system/scripts/uninstall.mjs -y
```

Show the output to the user. If `~/.agenfk-system` does not exist, inform the user that AgenFK does not appear to be installed.
